# Entity: series

main: kit: entity: series: {

  alias: field: {}

  fields: [
    {
      name: "cards"
      req: false
      short: "List of card IDs belonging to this series/archetype"
      type: "`$ARRAY`"
    }
    {
      name: "name"
      req: false
      short: "Series/archetype name in multiple languages"
      type: "`$OBJECT`"
    }
  ]

  name: "series"
  op: {
    list: {
      name: "list"
      points: [
        {
          method: "GET"
          orig: "/data/series/list.json"
          parts: [
            "data"
            "series"
            "list.json"
          ]
          select: {
            "$action": "list"
          }
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
