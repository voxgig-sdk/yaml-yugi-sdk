# Entity: aggregation

main: kit: entity: aggregation: {

  alias: field: {}

  name: "aggregation"
  op: {
    load: {
      name: "load"
      points: [
        {
          method: "GET"
          orig: "/cards.yaml"
          segments: [
            {
              lit: "cards.yaml"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
        {
          method: "GET"
          orig: "/rush.yaml"
          segments: [
            {
              lit: "rush.yaml"
            }
          ]
          transform: {
            req: "`reqdata`"
            res: "`body`"
          }
        }
      ]
    }
  }



}
